# Progressive capabilities become a Pi extension

SubAgents retains progressive disclosure as a trusted Pi Capability Extension built on Pi's tool catalog, active-tool controls, extension events, and custom tools. Pi Core remains the sole tool loop and source of tool definitions; after behavioral parity is established, the legacy capability runtime, function-calling tool loop, and duplicate schema registry are removed rather than wrapped around Pi.
