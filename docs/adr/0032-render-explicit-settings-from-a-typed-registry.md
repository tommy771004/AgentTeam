# Render explicit settings from a typed registry

The Electron settings experience is driven by a typed Settings Registry populated by Pi adapters and Extension Pack manifests. Users configure named, explained, validated controls such as selectors, toggles, ranges, model pickers, and tool lists, with dedicated React panels only for genuinely complex workflows; raw JSON, internal Pi keys, and manual configuration-file editing are not product interfaces.
