# 03 — zh-TW 原樣遷入：composer 與對話域

**What to build:** 這個域（composer 及其進階區、對話氣泡、建議卡、run 面板與後續動作）的所有介面字串改走 `t(key)`，語言檔以現行文字原樣入檔。使用者這一側完全無感——文案、順序、標點與現在逐字相同（視覺零 diff）；改變的是這些字串從此可以被翻譯。

**Blocked by:** 01

**Status:** 可交給代理

- [ ] 本域所有 UI 字串改走 `t(key)`，key 依頁面／元件分域命名
- [ ] zh-TW 語言檔文字與遷入前逐字相同
- [ ] 帶變數的字串以 `{param}` 內插表達，不用字串相接
- [ ] 對帳 smoke 全綠（無孤兒 key、無未宣告使用點）
- [ ] `npm run build`、smoke、元件測試全綠
