# 05 — 「事件」tile 改為預填建立事件規則

**What to build:** 使用者在 composer 輸入一句話後點「事件」，就地開啟建立主動事件規則的表單：目標欄以輸入文字預填，來源與關鍵字給出可選的建議值（而不是要使用者憑空想）。建立成功後表單關閉、對話內留下系統訊息，規則立刻出現在自動化頁的事件清單中。取消什麼都不建立。與排程共用同一套表單外殼與建立後回饋，兩個 tile 的體驗一致。

**Blocked by:** 04

**Status:** resolved

- [x] 點「事件」開啟建立表單，目標欄預填，來源／關鍵字有建議預設值
- [x] 建立成功後對話內出現系統訊息，自動化頁事件清單同步可見
- [x] 取消不建立任何東西，composer 輸入內容保留
- [x] 與排程 tile 共用表單外殼，兩者視覺與互動一致（無重複實作）
- [x] smoke（純邏輯）：事件建立 payload 組裝
- [x] 元件測試：預填、建立、取消三條路徑

## Answer

「事件」格共用同一張 sheet 與同一組建立後回饋：`buildEventDraft` 預填目標，來源建議只列這個 app 真的會送進 matcher 的 `webhook.http` / `monitor`（不虛構 `email.received`），可自由輸入。建立走 `scheduleStore.addEvent`。10 個元件測試涵蓋排程與事件兩路的預填、建立、取消、Esc、建立失敗留在表單並說明原因。
