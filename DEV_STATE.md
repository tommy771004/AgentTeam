# Development State

截至 2026-08-15，專案位於 `main`、基準 commit `89d26e0`，目前未提交的工作目標是讓 vendored Pi Core 的建置、Pi Host bundle 與 Electron 跨平台 resource layout 具備可重跑的驗證流程；已新增 `app/scripts/build-pi-vendor.mts`，以 vendor source hash 與 package version cache 建立隔離的 Pi build workspace，支援既有 model-data 的 offline build、失敗時轉 network build，完成後保留 runtime 依賴、複製必要 dist 並寫入本地 cache，`app/package.json` 也讓 `build:pi-host` 先建 vendor，並把 `vendor/pi` 與 `vendor/pi/node_modules` 放入 `extraResources`；`app/electron/piUserConfig.ts` 現在只有在 `SUBAGENTS_PI_SYNC_CLI_OAUTH=true` 時才會匯入 CLI OAuth，避免直接 host/protocol 測試讀取開發者家目錄憑證，`app/scripts/vite.pi-host.config.ts` 外部化 `node:os`，`smoke-pi-host-protocol.mts` 使用暫存 state，`smoke-pi-packaging.mts` 檢查必要 vendor artifacts 並驗證 macOS/Windows layout 的 session restart，`OPEN_DESIGN_INVENTORY.json` 仍為 361 筆、無 warnings、內容與 upstream commit 未變，只有生成時間更新；已確認 `npm run smoke:pi-host`、`npm run smoke`、`npm run build`、`npm run smoke:pi-migration` 全部成功，build 僅留下既有 Vite deprecation、renderer `node:*` externalization、large chunk/ineffective dynamic import warnings，npm 啟動時另有本機 `.zprofile` 引號錯誤但不影響 exit code；本輪沒有已確認的產品方案失敗，offline-to-network fallback 與真正 electron-builder 安裝包尚未測試，下一步是審閱並提交這組 app 變更，必要時補做 cache miss/離線 fallback 與實際打包驗證，再另行處理既有 build warnings。
