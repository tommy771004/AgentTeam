# 讓 CLI 訂閱認證餵進 Pi Core loop：把已同步的 OAuth 當 LLM credential，由 Pi 統一擁有 loop/approval/settlement。代價是失去 vendor agent 本身的工具鏈與行為，等於換一套執行語意——這會是需要先寫 ADR 的架構變更。

> run run_fa28a666-f21 · 2026-08-26T04:11:57.408Z

## 這次要做什麼

讓 CLI 訂閱認證餵進 Pi Core loop：把已同步的 OAuth 當 LLM credential，由 Pi 統一擁有 loop/approval/settlement。代價是失去 vendor agent 本身的工具鏈與行為，等於換一套執行語意——這會是需要先寫 ADR 的架構變更。

## 做了哪些決定

- Pi Core Host turn
- 驗收條件：Pi Core settlement returned

## 哪裡卡住或失敗

（這次沒有卡住的地方）

## 下次可以照著做的步驟

（這次沒有整理出可重複的步驟）
