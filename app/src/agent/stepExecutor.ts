/**
 * @deprecated Relocated to ./loop/stepIO.ts (Loop Runner deepening, ticket 02).
 * This shim exists only until ticket 04 removes the last import of this path.
 */
export * from './loop/stepIO.ts'
