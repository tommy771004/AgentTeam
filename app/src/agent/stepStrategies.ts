/**
 * @deprecated Relocated to ./loop/strategies.ts (Loop Runner deepening, ticket 02).
 * This shim exists only until ticket 04 removes the last import of this path.
 */
export * from './loop/strategies.ts'
